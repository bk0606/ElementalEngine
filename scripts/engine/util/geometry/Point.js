/**
 * Created by Kell on 11.02.14.
 */
define(
    function () {
        /**
         * 3D Point
         * @constructor
         */
        function Point(x, y, z) {
            this.x = x;
            this.y = y;
            this.z = z;
        }

        return Point;
    }
);